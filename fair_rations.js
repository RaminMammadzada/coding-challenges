const fairRations = (arr) => {
	let requiredSum = 0;
	let personI;
	personI + 1;
	personI - 1;

	for (let i = 0; i < arr.length; i += 1) {
		console.log(arr[i]);
		if (arr[i] % 2 != 0) {
			arr[i] = arr[i] + 1;
			arr[i + 1];
		}
	}
};

fairRations([2, 3, 4, 5, 6]);
